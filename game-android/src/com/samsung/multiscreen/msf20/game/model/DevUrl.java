package com.samsung.multiscreen.msf20.game.model;

/**
 *
 * This class will be checked in to the git repo and then added to the .gitignore.
 * Developers can put the local url they are using for testing here.
 *
 *
 */
public class DevUrl {
    public static final String url = null; 
    
    public static boolean hasUrl() {
        return (url != null);
    }
}
